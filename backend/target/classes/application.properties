# Server configuration
server.port=8080

# MySQL Database Configuration
# NOTE: Replace with your actual MySQL database credentials
spring.datasource.url=jdbc:mysql://localhost:3306/task_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=senthamizh0912

# Hibernate Configuration
# update checks schema and updates it. create-drop drops schema at end of session
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
